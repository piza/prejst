module.exports = {
    // 编译输出目录设置
    output: './build',

    // 模板使用的编码。（只支持 utf-8）
    charset: 'utf-8',

    // 是否压缩 HTML 多余空白字符
    compress: true,

    // 设置输出的运行时文件名
    runtime: 'template.js',

    // 是否输出为压缩的格式
    minify: true
};